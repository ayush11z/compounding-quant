__pycache__/
*.pyc
.venv/
.env

# Session continuity notes for AI assistants working across sessions -- not for the
# public repo.
CLAUDE.md

# Per-instance run artifacts (swebench's own logs + our sandbox logs) -- transient,
# regenerated by every run, not meaningful to version.
/logs/
results/logs/
sanity_check_bf16*.log

# Large/derivative artifacts — never belong in git
engines/**/*.engine
engines/**/*.plan
*.safetensors
*.bin
*.gguf
weights/

# Results data (schema/code tracked, data is not)
results/*.duckdb
results/*.parquet
results/raw/

# Env reports are machine-specific snapshots, not source
env_reports/*.txt

.DS_Store
